const createUSer = {
    first_name: 'required|string',
        last_name:  'required|string',
        email: 'required|string',
        password: 'required|string',
}

module.exports ={
    createUSer
}